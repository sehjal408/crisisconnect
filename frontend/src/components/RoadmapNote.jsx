// Roadmap notes are hidden for a cleaner, finished look (removed at the user's
// request). Restore the previous single-line render below to bring them back —
// callers still pass `items` / `week`, so nothing else needs to change.
export default function RoadmapNote() {
  return null;
}
